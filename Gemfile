source "https://rubygems.org"

# Tooling for the specs that guard data-index/configs.yml and the CI workflows.
# This repo otherwise stores only CI config; these are dev/test-only deps.
gem "rake", "~> 13.0"
gem "rspec", "~> 3.13"
