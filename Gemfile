source "https://rubygems.org"

# Tooling for the data-index config generator (bin/gen-data-index-config).
# This repo otherwise stores only CI config; these are dev/test-only deps.
gem "rake", "~> 13.0"
gem "rspec", "~> 3.13"
